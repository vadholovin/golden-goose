.hero#hero
  .container--fluid
    .hero__grid
      .hero__col.hero__col--left
        .hero__box
          .brand.brand--hero
            img(src="img/general/logo.png", alt="")
          button.button.button--light.button--video
            | смотреть видео
        
        .hero-slider__wrapper
          .hero__slider.hero-slider.js-hero-slider
            .hero-slider__item
              img(src="img/content/hero/img-1.jpg", alt="")
            .hero-slider__item
              img(src="img/content/hero/img-1.jpg", alt="")
            .hero-slider__item
              img(src="img/content/hero/img-1.jpg", alt="")
          .hero-slider__dots
            .slick-counter 01

        a.icon.icon-mouse.hero__down(
          href="#about"
        )

      .hero__col.hero__col--right.overlay(
        style="background-image: url(img/general/bg/img-1.jpg);"
      )
        form.hero__form.hero-form.form(
          action=""
        )
          .form-row
            label.hero-form__label(for="checkin")
              | Выезд
            .form-control__wrapper
              input.form-input#checkin(
                type="text",
                name="checkin",
                placeholder="19 июня 2019"
              )
              label.icon.icon-calendar(for="checkin")
          .form-row
            label.hero-form__label(for="checkout")
              | Заезд
            .form-control__wrapper
              input.form-input#checkout(
                type="text",
                name="checkout",
                placeholder="19 июня 2019"
              )
              label.icon.icon-calendar(for="checkout")
          .form-row
            button.button.button--arrow(
              type="submit"
            ) найти номер
              svg.arrow
                use(xlink:href="img/icons/arrow-r-golden.svg#arrow")
  
